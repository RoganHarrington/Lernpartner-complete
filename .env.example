# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=lernpartner
POSTGRES_PASSWORD=changeme
POSTGRES_DB=lernpartner

# MinIO (S3-compatible storage)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=lernpartner-media
MINIO_BUCKET_ARTIFACTS=lernpartner-artifacts
MINIO_BUCKET_BACKUPS=lernpartner-backups

# Auth
JWT_SECRET=changeme-use-a-long-random-string
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Encryption (for sensitive user data at rest)
ENCRYPTION_KEY=changeme-32-byte-hex-key

# Server
SERVER_PORT=3000
CLIENT_URL=http://localhost:5173

# Client
VITE_API_URL=http://localhost:3000/api
